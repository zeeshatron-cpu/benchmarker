__pycache__/
*.py[cod]
.venv/
venv/

# Local config and captured auth — never commit secrets/sessions.
config.yaml
config.json
*_auth.json

# API keys — NEVER commit. Only .env.example (with blank values) is tracked.
.env
.env.*
!.env.example

# Generated artefacts.
logs/*.jsonl
reports/*.md
queries/queries.jsonl
